#!/bin/bash
set -u

script_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")/../scripts" && pwd)
# shellcheck source=/usr/local/emhttp/plugins/yarr/scripts/yarr-array-stop-event.sh
source "$script_dir/yarr-array-stop-event.sh" || exit 1
yarr_array_stop_event
