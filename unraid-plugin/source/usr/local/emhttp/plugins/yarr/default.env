# Optional Yarr service credentials are managed through the settings UI.
# Network bearer authentication requires 256 bits: 64 hex or 43 base64url characters.
# This file is intentionally empty on a fresh install.
