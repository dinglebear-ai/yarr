name: Temporary 0728 diagnostics

on:
  push:
    branches:
      - fix/mcp-2026-07-28-alignment

permissions:
  contents: read

jobs:
  diagnose:
    if: github.actor == 'never-run-temporary-diagnostics'
    runs-on: ci-pool-rust
    steps:
      - run: echo disabled
